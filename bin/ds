#!/usr/bin/env bash

echo "after REPL starts, do (require '[hkimjp.datascript :as ds])"
echo "then, try ds/version"

clj -J--enable-native-access=ALL-UNNAMED -Sdeps '{:deps {io.github.hkimjp/datascript-storage-javatime {:git/tag "0.7.4" :git/sha "f5c3196"}}}'
